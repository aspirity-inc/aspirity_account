namespace $.$$ {

	const { rem, per  } = $mol_style_unit

	$mol_style_define( $aspirity_account_invoice_page , {

		flex: {
			basis: rem(54),
			shrink: 0,
		},

		Logo: {
			maxWidth: per(25),
			margin: $mol_gap.block,
		},

	} )

}
